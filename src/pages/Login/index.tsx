import * as Styled from "./styles";
import LoginModal from "components/LoginModal"

const Login = () => {
    return (
        <Styled.Background>
            <LoginModal />
        </Styled.Background>
    )
}

export default Login